{
  "name": "leveling-ai",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/ && cd backend && npm install && npx prisma generate",
    "start": "node backend/index.js"
  },
  "devDependencies": {
    "@types/react": "^19.0.10"
  }
}
